{
  "name": "portifolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --mode dev",
    "build:dev": "astro build --mode dev",
    "build": "astro build --mode prod",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build:dev && firebase deploy --only hosting --project live",
    "deploy:live": "npm run build:dev && firebase deploy --only hosting --project live"
  },
  "dependencies": {
    "astro": "^6.1.2",
    "firebase": "^12.11.0"
  },
  "devDependencies": {
    "heic-convert": "^2.1.0"
  }
}
