{
  "name": "portifolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "firebase:login": "firebase login",
    "firebase:init": "firebase init",
    "deploy:dev": "npm run build && firebase deploy --project dev-portfolio",
    "deploy:prod": "npm run build && firebase deploy --project prod-portfolio"
  },
  "dependencies": {
    "astro": "^6.0.8",
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "firebase-tools": "^13.5.0"
  }
}